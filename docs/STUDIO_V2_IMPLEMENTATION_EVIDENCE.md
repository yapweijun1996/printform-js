# Studio v2 Resumed Implementation Evidence

Date: 2026-09-10. Scope: current review revision `a4caf93857669e05d0d521567ecf5ab6f4389df5` (prior application review baseline `e4302009e461ae398476ec63043c888cd19a07a`); Windows Firefox runner verification commit `93a4f2055f9e30dc75f7df06c9e4ba3c1baebff0`; P0 confirmation-test commit `59f4fe7`; documentation status follows this source revision.
Status: **Partial**. Superseding [handoff](STUDIO_V2_AGENT_HANDOFF.md): S16 is Done at G1-G5/100%, S17 is at G1/10%; plan 76.7%, Done 16/21, P0 35/35 Pass, PI 4/6. The current S16 evidence requalifies the deferred-close and pending-open lifecycle deltas; the review commit and `npm run doctor` are now verified. See [documentation review](STUDIO_V2_HANDOFF_REVIEW.md).
The [P0 register](STUDIO_V2_P0_ACCEPTANCE.md) alone owns case closure.
Latest P0 register: **35 Pass, 0 Fail, 0 Not run**. The focused 13-07 run now closes the browser Demo request/disclosure path after origin registration; later PI/release evidence remains incomplete.

## Sequential handoff review (2026-09-08)

Source reviewed: `main` at `3a1a7dbb93de5edd7984842896afdaab42a92bed` plus the existing uncommitted worktree; no clean-commit or release claim. A prior same-session serial command `node node_modules/vitest/vitest.mjs run --maxWorkers=1 --no-file-parallelism --reporter=dot` passed **103 files / 536 tests**, exit 0, in 149.53 seconds. The earlier doctor result below remains a separate 535-test historical build/validator run; the current doctor result is recorded below.

Code Slice review reconfirmed maximum-array row counting, component `repeatHeader` writing a root flag, current AGRUN loading/runtime creation, existing commit-outcome resolution and source-editor Quality routing. The prior direct synthetic probe returned 400 for two 400-row arrays. At that 2026-09-08 snapshot, CI listed two static pilots while doctor listed three; S20 preparation later adds the third CI entry, with no remote run claimed. These are bounded review findings, not a new full runtime/browser audit.

S01 G1/G2: created the [execution plan](STUDIO_V2_EXECUTION_PLAN.md), [DoD](STUDIO_V2_DEFINITION_OF_DONE.md), live TASK gate ledger and [Goal Prompt](STUDIO_V2_GOAL_PROMPT.md); connected the existing requirement/checklist owners. Application source was not amended. P0 Pass records and prior implementation remain intact; subsequent steps start with uncredited delivery gates, not an assertion of missing implementation.
S01 G3/G4: document validation passed for 13 amended/new Markdown files and 176 local links; S01-S21 IDs/dependencies, gate arithmetic, 35 P0 cases with eight unchanged Pass records, <=300 lines per file and the 1962-character Goal Prompt passed. Scoped `git diff --check` passed. Amendment-only comparison against captured document preimages confirmed that changes preserve prior implementation/case records and replace only the intended sequencing/status wording. No new application build/browser/provider/print run is claimed or required for this documentation-only amendment.
S01 G5: documentation handoff closed at 100%; TASK records the verified gates. S02/PI-00 is the next recorded package below. The new bounded plan was 1/21 steps Done (4.8% closure) at handoff; current S02 closure is recorded separately and does not revise lifetime product completeness.

## S02 / PI-00 qualification (2026-09-08)

Status: **Done, 100%** (`G1/G2/G3/G4/G5 = 10/50/80/95/100`). This is an isolated qualification artifact; the Studio production entry still uses AGRUN while PI-03 through PI-05 remain to integrate and qualify the replacement.

- G1: reviewed the current AGRUN entry, runtime/session callers and contract-test owners before editing. The actual PI path is isolated at `studio-v2/pi-00/`; no existing canonical project, revision/CAS, approval/export, privacy sink, record namespace or protocol contract was changed.
- G2: added exact direct pins `@earendil-works/pi-agent-core@0.85.1` and `@earendil-works/pi-ai@0.85.1`; the root build engine is `>=22.19.0` to honor the published dependency engine. `package-lock.json` records the registry tarballs, `MIT` notices and integrities: core `sha512-hIXIP3eAWueAYiAl8aMvWCvvZ8Q5gT3Dip5bE5uJyIGh4+YlWRjtMLI4BaeoXoSs93zndjue61u1B/vhefLnuA==`; ai `sha512-+VgVIJDkDO2efYJKEEqvPTH4zmnIaXdAppGbO+vKFA9qy5PdhFiAenuFAkU+oiCSfOC4dMHDyrjdQeL4ZoC5CQ==`. Both use the public root imports and the reviewed upstream source commit `b2602be77cb7b0de45dd616407fd210daa48aa75`.
- G3: `node scripts/build-site.mjs` passed and generated `site-dist/studio-v2/pi-00/qualification-entry.js` as an ES static bundle of **823,439 bytes**, with `qualification-manifest.json` recording `appBackend: false` and `providerProxy: false`. Bundle inspection found no bare package imports, no static or direct dynamic `node:` import, `eval(` or `new Function` workaround; the package's unused browser-safe Node capability strings are not executed by this qualification.
- G4: the single PI-00 acceptance case passed **3/3** serially: `npx playwright test e2e/studio-v2-pi-00.spec.js --project=chromium --project=firefox --project=webkit --workers=1`. Each engine executed the actual `AgentHarness.create`, `MemorySessionRepo`, lane, deterministic faux provider/tool cycle (`completed`) and real lane abort (`aborted`); the request log contained no `/api`, `/mcp`, `/transaction`, `/gateway` or `/proxy` route.
- G5: TASK, execution plan, PI migration plan and boundary/index/production summaries now distinguish PI-00 qualification from the unchanged AGRUN production entry. `P0` remains **8/35 Pass, 0 Fail, 27 Not run**; `PI` is **1/6 Done**; release approval, live BYOK/provider evidence and print/platform evidence remain open.

The first Chromium attempt failed its observation assertion because abort was requested immediately after `run_start`, before the faux provider call counter advanced (`providerCalls: 0`). The Harness still returned `aborted`; the qualification was tightened to wait for provider startup, then the final Chromium/Firefox/WebKit run passed with `providerCalls: 1`. This corrected an evidence-timing defect rather than discarding a product failure. Live BYOK, production-entry cutover, provider CORS, physical print, platform certification and deployment remain unrun by design.

## S03 / PROD-13 M1 destination mapping (2026-09-08)

Status: **Done, 100%** (`G1/G2/G3/G4/G5 = 10/50/80/95/100`). This closes the application-controlled destination inventory, not external Provider/OS/deployment retention or broader P0.

- G1: traced every `studio-v2` persistence/network producer and the transaction-server path before editing; froze 13-01..13-08 evidence and preserved canonical/CAS/approval/export and old-record rules. Docs-only change; no application behavior claim changed.
- G2: amended authoritative `STUDIO_V2_DATA_POLICY.md` with exact keys/stores/cache/server/log/explicit-artifact destinations and transition ownership; mapped each existing case to DP-S/T/L rules. No source runtime edit or alternate sink.
- G3: bounded source audit found no other Studio app writes to `sessionStorage`, cookies, `sendBeacon`, XHR, `BroadcastChannel` or Cache Storage outside `sw.js`; document link/ID/count/line validation passed; focused persistence/SW/asset/server checks passed (**5 files / 19 tests**, plus E13-SERVER **1 file / 8 tests**).
- G4: existing current browser evidence for 13-01..13-08 remains valid because no behavior source changed; the docs-only DoD path requires no new browser/build run. No live provider/private data/print/deployment evidence is claimed.
- G5: synchronized TASK, P0 register, production plan, data policy and evidence. P0 remains **8/35 Pass, 0 Fail, 27 Not run**; PI is **1/6 Done**; release approval remains open.

## S04 / PROD-05 bound-table row limits (2026-09-08)

Status: **Done, 100%** (`G1/G2/G3/G4/G5 = 10/50/80/95/100`). This closes the local bound-table limit and row-order package; repeat-rule granularity, broader P0 and release certification remain open.

- G1: traced `countRows`, runtime validation, `data-pf-each` binding and formatter callers; identified the existing maximum-array defect and preserved the compatible `maxRows` manifest field, row-conservation checks and 1000-row performance evidence as non-limit evidence.
- G2: added a shared row-limit owner. `maxRows` is the aggregate bound-table limit and `maxRowsPerTable` is the per-table limit, both 500 by default; only actual `.prowitem`/explicit-table repeats are counted. Generic repeats remain supported and reported separately, while runtime row conservation uses table rows.
- G3: the current affected unit run passed **4 files / 25 tests**; the preceding full serial pretest passed **105 files / 545 tests**. `npm run build:assets` and `node scripts/build-site.mjs` also passed, generating the 161-entry static shell. Boundary assertions cover 400+400, exact 500, limit+1 and a 1000-row unbound nested array.
- G4: `e2e/studio-v2-prod05.spec.js` passed **3/3** serially in Chromium, Firefox and WebKit. Actual browser formatting rendered 500 table rows in order (`0..499`) with no browser errors; the two-table aggregate and clear per-table/aggregate failures were observed in-browser.
- G5: synchronized TASK, production plan, protocol, design/roadmap notes and this evidence. P0 remains **8/35 Pass, 0 Fail, 27 Not run**; PI is **1/6 Done**; no deployment, live provider, OS retention or print-platform certification is claimed.

## S05 / PROD-06 repeat-rule granularity (2026-09-08)

Status: **Done, 100%** (`G1/G2/G3/G4/G5 = 10/50/80/95/100`). This closes the repeat-rule package; full scope, broader P0 and release certification remain open.

- G1: traced FormSpec markup, pagination operation/scope admission, formatter row-header ownership and render diagnostics; selected a compatible local override while retaining the legacy root default.
- G2: `repeatHeader` now writes `data-pf-repeat-rowheader` only on the addressed table-header and updates that FormSpec component; the root `data-repeat-rowheader` remains the legacy default. Non-table repeatHeader targets fail closed.
- G3: the affected unit set passed **6 files / 62 tests**; the complete serial pretest passed **105 files / 548 tests**. `npm run build:assets` and `node scripts/build-site.mjs` passed; the static shell contains 161 entries.
- G4: `e2e/studio-v2-prod06.spec.js` passed **3/3** serially in Chromium, Firefox and WebKit. The browser proved A=`n`, B=`y` stays independent across continuation pages, while a legacy root `n` still disables repeated headers; no browser errors were observed.
- G5: synchronized TASK, production plan, execution/design/spec/roadmap/protocol notes and agent output shape. P0 remains **8/35 Pass, 0 Fail, 27 Not run**; PI is **1/6 Done**; no deployment, live provider, OS retention or physical print evidence is claimed.

## S06 / PROD-01 scope acceptance (Done, 2026-09-08)

Status: **Done, 100%** (`G1/G2/G3/G4/G5 = 10/50/80/95/100`). This closes the eight scoped PROD-01 cases; broader PROD-02/03/13 acceptance, PI work and release certification remain open.

- G1: traced preview selection, FormSpec identity derivation, document-context state, render-controller message ownership, scope-option mapping, gateway/domain enforcement, legacy fallback and the existing scope/entry tests. Frozen exit cases were P0 01-01..08; required checks covered focused unit/contract coverage, real-browser selection/transaction behavior, cross-entry parity and legacy/whole-document transitions.
- G2: the current scope implementation covers preview-only FormSpec annotations, current token/revision selection messages, structural target metadata, component/table mapping, source-scope option refresh, actionable localized scope errors, pre-transaction whole-array validation, indirect repeatHeader locality, scope-bound candidate approval, project-replacement cleanup, embedded/WebMCP/CDP/domain parity and raw-source blocking. The 01-08 path confirms the unmodified legacy sample resolves through `createLegacyFormSpec`; ambiguous selectors stay blocked under table scope, while explicit document scope permits only the intentional whole-document operation through existing transaction/validation/approval checks and component restrictions return after switching back.
- G3: the focused S06 set passed **46/46** across nine files (`form-spec`, `agent-scope`, `agent-scope-options`, `preview`, `render-controller`, `agent-document-context`, `ui-i18n`, `agent-entry-parity`, `gateway-admission`). `npm run build:site` passed **105 files / 551 tests**, generated the static site artifact, and `npm run check` passed. The full negative/boundary set includes wrong target/category, mixed arrays, stale scope/document, indirect effects, entry parity and legacy ambiguity.
- G4: the case-specific browser runs passed **3/3 for each of 01-01 through 01-08** across Windows Chromium, Firefox and WebKit, for **24/24** PROD-01 browser case executions. 01-08 observed the real panel source editor, legacy adapter, ambiguous table rejection, explicit whole-document preview/private Apply and restored component rejection. Firefox's existing AGRUN CSP eval console diagnostic is recorded; no functional pageerror or unknown console diagnostic occurred.
- G5: TASK, the P0 case register, production plan, direction/index/roadmap summaries and release checklist now agree on 01-01..08 Pass, S06 `xxxxx`/100%, plan closure **28.6% (600/21)** and P0 **16/35 (45.7%)**. No deployment, publish, live provider, physical print or maintainer release approval is claimed.

## S07 / PROD-02 02-01/02-02/02-03/02-04/02-05/02-06/02-07/02-08 Apply paths (Done, 2026-09-09)

Status: **Done, 100%** (`G1/G2/G3/G4/G5 = 10/50/80/95/100`). All eight PROD-02 cases are now closed; later P0, PI and release gates remain open.

- G1: traced the shared chat/Review runtime, private UI approval session, transaction/CAS boundary, commit-outcome resolver and the existing workflow/panel/commit tests; froze the eight PROD-02 cases and their browser, unit and cross-entry checks.
- 02-01: the real Studio chat panel in Preview mode created a valid brand-colour candidate, held the committed revision/project hash/template source unchanged through all callbacks, then used the private UI Apply path to commit once and leave the Applied/Undo card.
- 02-02: the real Review panel flow held repair 1 pending, committed it once, generated a different repair-2 proposal at revision 1, required a second Apply, and only then completed the review at revision 2.
- G3 supporting checks: `node --check` passed for both new cases; the focused union of `agent-workflow`, `agent-panel-runtime`, `commit-boundary`, `agent-layout-review` and `agent-terminal-state` suites passed **31/31**. This does not satisfy the complete S07 focused matrix.
- G4: `e2e/studio-v2-p0-prod02-01.spec.js` passed **3/3** serially across Chromium, Firefox and WebKit. The test recorded one runtime run, one preview action, all callbacks, unchanged pending state, one committed revision and no functional pageerror; the known Firefox AGRUN CSP diagnostic remains explicitly allowed.
- G4: `e2e/studio-v2-p0-prod02-01.spec.js` and `e2e/studio-v2-p0-prod02-02.spec.js` each passed **3/3** serially across Chromium, Firefox and WebKit. The controls recorded the chat callbacks and the three-pass Review repair/approval sequence with no functional pageerror; the known Firefox AGRUN CSP diagnostic remains explicitly allowed.
- 02-03: the real panel/browser control passed **3/3** across Chromium, Firefox and WebKit. Page-global, ordinary bound-session, WebMCP and Chromium CDP callers lacked `executeHuman`; forged approval fields returned `HUMAN_APPROVAL_REQUIRED`, while the UI-owned session still required its panel token. Tampered token, candidate hash, expired lease and stale revision were rejected; one valid UI Apply committed revision 2. The isolated in-memory bus preserved revision 0 until the valid Apply. The known Firefox AGRUN CSP diagnostic is explicitly allowed; no functional pageerror or unknown diagnostic occurred.
- G3 supporting checks for 02-03: the focused transaction-recovery, production-boundary and workflow suites passed **37/37**; the new lease regression confirms an already-expired record returns `LEASE_EXPIRED` without stale rollback conflict.
- 02-04: the real chat panel/browser control passed **3/3** across Chromium, Firefox and WebKit. A delayed controlled Harness response began in Auto, switched to Preview before proposal arrival, and kept revision/brand color unchanged; switching back to Auto kept the pending card without committing, while only a new explicit Preview-mode Apply committed once. Focused mode-runtime tests passed **8/8**; no functional pageerror or unknown diagnostic occurred.
- 02-05: the real chat panel/browser control passed **3/3** across Chromium, Firefox and WebKit. Stop cancelled the first delayed run before proposal creation; after release, only its first late token was observed and no late phase/completed event was consumed. Retry created one pending candidate, Discard removed it before Apply, and the later retry callbacks completed without changing revision or brand color; no card said Applied. Supporting `agent-workflow`, `agent-panel-runtime` and `agent-terminal-state` suites passed **23/23**; no functional pageerror or unknown diagnostic occurred.
- 02-06: the real panel/browser control passed **3/3** across Chromium, Firefox and WebKit. A double Apply dispatch made one approval attempt while the lost Apply response was reconciled by transaction lookup, committing at most one revision; the confirmed Applied card reported the real revision. Post-commit validation unavailability kept the card Applied with an unavailable-validation status; an injected post-write interruption kept the candidate in recovery-required with no Apply/Discard and no resubmission. `npm run build:site` passed **105/553** and focused commit/panel tests passed **13/13**; no functional pageerror or unknown diagnostic occurred.
- 02-07: the real panel/browser control passed **3/3** across Chromium, Firefox and WebKit. Auto applied one explicitly eligible in-scope brand-colour operation once and completed one clean layout-review pass; ineligible and mixed batches stayed pending without an Auto Apply control or revision change; invalid and out-of-scope operations were rejected. The human source editor committed once, demoted the project to untrusted and blocked the public mutation with `UNTRUSTED_READ_ONLY`. Supporting eligibility/scope/operation/boundary/panel tests passed **73/73**; no functional pageerror or unknown diagnostic occurred.
- 02-08: the real panel/browser control passed **3/3** across Chromium, Firefox and WebKit. The actual runtime loaded and parsed `printform-designer.md` into AGRUN `agentSkills` and supplied the policy prompt to the Harness. Preview kept the candidate pending; a held CommandBus Apply showed Applying and then Applied only after the real commit, and a passing layout review showed the explicit human print-preview/export requirement. AI Apply and review caused zero download clicks; dismissing the production confirmation caused no download, while accepting it caused exactly one HTML download. No functional pageerror or unknown diagnostic occurred; the known Firefox AGRUN CSP diagnostic remains explicitly allowed.
- G2: current runtime prompt delivery, action policy, panel state projection and human-only export implementation satisfy the complete eight-case scope; no new production source amendment was required for 02-08.
- G3: the focused prompt/layout/command/export regression command passed **45/45** across `agent-prompt-policy`, `agent-layout-review`, `layout-review`, `studio-file-export` and `command-bus` suites.
- G4: the 02-01 through 02-08 real-panel controls each pass **3/3** across Chromium, Firefox and WebKit; 02-08 specifically observed pending/applying/applied, review completion, confirmation dismissal and accepted download behavior.
- G5: evidence, TASK, P0 and affected authority documents are synchronized; no temporary production artifact, deployment, provider authorization or release approval was created. Plan closure is **33.3% (700/21)** with **7/21** steps Done; P0 is **24/35 (68.6%)** and PI is **1/6**.

### Case-specific evidence: PROD-01 01-01 — Pass

- Date: 2026-09-08; source: current uncommitted worktree based on `3a1a7dbb93de5edd7984842896afdaab42a92bed`; environment: Windows Playwright Chromium, Firefox and WebKit, one worker; fixture: synthetic Sales Invoice only.
- The sandboxed preview annotated rendered table-header instances with the stable FormSpec ID `table-default-header`. Clicking the first instance updated the context label and target metadata with the same component ID, `tableId=default`, structural type/role and `source=preview`, and selected the matching default table scope. Repeated page instances shared the identity rather than creating a new target.
- A table-scoped `set_brand_color` request was rejected with `SCOPE_VIOLATION`; the selection stayed unchanged and committed revision stayed at `0`. This demonstrates category rejection without silently changing the selected component.
- `e2e/studio-v2-p0-prod01-01.spec.js`: **3/3**; focused unit set: **25/25**; preceding serial build/test hook: **105 files / 550 tests**. No credential, live provider, real business data, deployment or print-platform claim.
- Limitation: Firefox reported the pre-existing AGRUN `eval` CSP console diagnostic from `vendor/agrun.min.js`; there was no functional `pageerror` or other console diagnostic. CSP/AGRUN remains unchanged and is deferred to PI-05/S18; the diagnostic is not hidden by the test or evidence record.

### Case-specific evidence: PROD-01 01-02 — Pass

- Date: 2026-09-08; source: current uncommitted worktree based on `3a1a7dbb93de5edd7984842896afdaab42a92bed`; environment: Windows Playwright Chromium, Firefox and WebKit, one worker; fixture: synthetic Sales Invoice only.
- The actual Studio source editor created the approved fixture with repeating table A and an independent static PTAC table B. The rendered preview selected A through its FormSpec component annotation and kept the same table scope while one deterministic in-page Harness action proposed the allowed column-width edit.
- Before Apply, the candidate reported only `changedSections=["templateHtml"]`; A showed the requested widths, B and the captured unrelated PTAC text/values were unchanged, and the canonical editor source remained at the approved baseline. The real private UI Apply control then produced exactly one revision increment and a changed project hash; the candidate card disappeared and the applied card exposed Undo.
- `e2e/studio-v2-p0-prod01-02.spec.js` passed **3/3** across Chromium, Firefox and WebKit. The deterministic control recorded exactly one Harness run and one `printform_preview_changes` action. Public `get_revision` before/after confirmed the expected revision/hash/source transition; transaction-history counts were intentionally not used because the public gateway is not the private UI session's history view.
- The rebuilt `npm run build:site` result passed **105 files / 550 tests** and generated the site artifact. No credentials, live provider, real business data, deployment or print-platform claim was used.
- Limitation: the deterministic in-page action provides reproducible browser transaction evidence, not live-provider or PI certification. Firefox retained the known AGRUN `eval` CSP console diagnostic from `vendor/agrun.min.js`; no functional `pageerror` or unknown console diagnostic occurred. CSP and AGRUN remain unchanged and the runtime gate stays with PI-05/S18.

### Case-specific evidence: PROD-01 01-03 — Pass

- Date: 2026-09-08; source: current uncommitted worktree based on `3a1a7dbb93de5edd7984842896afdaab42a92bed`; environment: Windows Playwright Chromium, Firefox and WebKit, one worker; fixture: synthetic Sales Invoice with table A and static table B.
- After selecting table A in the preview, the deterministic in-page Harness requested a column-width operation targeting table B and then a `set_brand_color` operation. Both requests reached the existing gateway/domain scope boundary with the trusted `table:a` context.
- Both requests returned `SCOPE_VIOLATION`; the panel showed the actionable localized scope message, created no candidate/Apply control, preserved table-A target metadata and scope, and left revision, project hash and canonical editor source unchanged.
- `e2e/studio-v2-p0-prod01-03.spec.js` passed **3/3** across Chromium, Firefox and WebKit. The deterministic control recorded two runs/two actions and two scope failures. `tests/studio-v2/ui-i18n.test.js` passed **7/7** for the error projection, and the rebuilt `npm run build:site` passed **105 files / 550 tests**. No credentials, live provider, real business data, deployment or print-platform claim.
- Limitation: the provider action was deterministic and in-page for reproducibility; this is real-browser rejection/UI evidence, not live-provider or PI certification. Firefox retained the known AGRUN `eval` CSP console diagnostic from `vendor/agrun.min.js`; no functional `pageerror` or unknown console diagnostic occurred. CSP and AGRUN remain unchanged and the runtime gate stays with PI-05/S18.

### Case-specific evidence: PROD-01 01-04 — Pass

- Date: 2026-09-08; source: current uncommitted worktree based on `3a1a7dbb93de5edd7984842896afdaab42a92bed`; environment: Windows Playwright Chromium, Firefox and WebKit, one worker; fixture: synthetic Sales Invoice with table A and static table B.
- With table A selected and `table:a` active, the deterministic in-page Harness submitted two complete mixed batches: allowed A column widths plus forbidden B column widths, and allowed A column widths plus forbidden Theme mutation.
- Expected/actual: each complete two-operation array reached the trusted scope boundary unchanged and was rejected atomically with `SCOPE_VIOLATION`. No candidate, Apply control or candidate preview was created; the selected A target/scope stayed intact and revision, project hash and canonical editor source remained unchanged after both attempts.
- Evidence: `e2e/studio-v2-p0-prod01-04.spec.js` passed **3/3** across Chromium, Firefox and WebKit. The control recorded two runs, two actions, both original two-operation arrays and two scope failures. `tests/studio-v2/agent-scope.test.js` passed **7/7**, including zero transaction records for the mixed batch; the rebuilt site run passed **105 files / 550 tests**. No credentials, live provider, real business data, deployment or print-platform claim.
- Limitation: the provider action was deterministic and in-page for reproducibility; this is real-browser atomic-rejection evidence, not live-provider or PI certification. Firefox retained the known AGRUN `eval` CSP console diagnostic from `vendor/agrun.min.js`; no functional `pageerror` or unknown console diagnostic occurred. CSP and AGRUN remain unchanged and the runtime gate stays with PI-05/S18.

### Case-specific evidence: PROD-01 01-05 — Pass

- Date: 2026-09-08; source: current uncommitted worktree based on `3a1a7dbb93de5edd7984842896afdaab42a92bed`; environment: Windows Playwright Chromium, Firefox and WebKit, one worker; fixture: synthetic Sales Invoice with table A and static table B.
- The host selected the concrete FormSpec component scope `component:table-a-header`. A shared Theme mutation and a `set_column_widths` selector matching both table A and table B were attempted through the same deterministic in-page Harness action path.
- Expected/actual: both document-wide effects were rejected with `SCOPE_VIOLATION`; the panel showed the actionable localized scope message, created no candidate, and left the selected component, revision, project hash and canonical source unchanged. A separate `set_pagination_rule` request with the same component ID was accepted only as the bounded local effect: candidate preview showed A's `data-pf-repeat-rowheader="y"`, B had no local repeat flag, and A repeated across continuation pages even though the legacy root `data-repeat-rowheader="n"` disabled the global default. This proves effective output locality rather than trusting a componentId field alone.
- Evidence: `e2e/studio-v2-p0-prod01-05.spec.js` passed **3/3** across Chromium, Firefox and WebKit. The control recorded three Harness runs/actions, two `SCOPE_VIOLATION` failures and one local candidate; no page error or unknown console diagnostic occurred. No credentials, live provider, real business data, deployment or print-platform claim was used.
- Limitation: the provider action was deterministic and in-page for reproducibility; this is real-browser scope/effective-output evidence, not live-provider or PI certification. Firefox retained the existing AGRUN `eval` CSP console diagnostic from `vendor/agrun.min.js` where applicable; CSP and AGRUN remain unchanged and the runtime gate stays with PI-05/S18.

### Case-specific evidence: PROD-01 01-06 — Pass

- Date: 2026-09-10; source: current local worktree after test-only confirmation handling commit `59f4fe7`; environment: Windows Playwright Chromium, Firefox and WebKit, one worker; fixture: synthetic Sales Invoice followed by synthetic Purchase Order import.
- The host selected the Sales Invoice table scope. A deterministic in-page Harness action created a candidate, then the scope changed before Apply. The real private UI Apply path rejected the old proposal through the existing transaction context guard; a fresh preview at the current revision was then applied, followed by a second pending candidate and an actual HTML import.
- Expected/actual: the old proposal returned `SCOPE_CHANGED`, the panel projected the localized “preview again” message, the candidate was cleared and revision/hash/source stayed at the baseline. The fresh proposal applied once and advanced the revision by one. Document replacement cleared the pending candidate, reset scope to `all`, rendered the imported Purchase Order as Printable and did not inherit the old candidate or scope.
- Evidence: `e2e/studio-v2-p0-prod01-06.spec.js` passed **3/3** across Chromium, Firefox and WebKit after explicitly accepting the intentional import-confirmation dialog. The control recorded three Harness runs/actions and three successful action results; no page error or unknown console diagnostic occurred. `tests/studio-v2/ui-i18n.test.js` passed **7/7**; the rebuilt `npm run build:site` passed **105 files / 551 tests**. No credentials, live provider, real business data, deployment or print-platform claim was used.
- Limitation: the provider action was deterministic and in-page for reproducibility; this is real-browser stale-context/UI evidence, not live-provider or PI certification. Firefox retained the known AGRUN `eval` CSP console diagnostic from `vendor/agrun.min.js`; CSP and AGRUN remain unchanged and the runtime gate stays with PI-05/S18.

### Case-specific evidence: PROD-01 01-07 — Pass

- Date: 2026-09-08; source: current uncommitted worktree based on `3a1a7dbb93de5edd7984842896afdaab42a92bed`; environment: Windows Playwright Chromium, Firefox and WebKit, one worker; fixture: synthetic Sales Invoice only.
- The browser installed the existing embedded gateway and WebMCP adapter over one synthetic CommandBus, then used the actual Playwright CDP session in Chromium through `CdpStudioClient`. Each entry submitted the same allowed table-column preview and the same forbidden theme operation with forged document/table/component scope fields; the direct domain guard was exercised against the same semantic scope.
- Expected/actual: all entries accepted the allowed table preview and rolled it back without advancing revision. All entries rejected the forged forbidden request with `SCOPE_VIOLATION`; revision stayed `0`. Embedded and Chromium CDP returned `AGENT_RAW_SOURCE_BLOCKED` for the raw-source command, while WebMCP registered exactly 35 tools and no raw-source tool. The direct domain guard returned the same decisions.
- Evidence: `e2e/studio-v2-p0-prod01-07.spec.js` passed **3/3** across Chromium, Firefox and WebKit; Chromium exercised CDP in addition to embedded/WebMCP/domain, while Firefox/WebKit exercised the browser-side embedded/WebMCP/domain paths. `tests/studio-v2/agent-entry-parity.test.js` passed **4/4** and `tests/studio-v2/agent-scope.test.js` passed **7/7**. The rebuilt site run passed **105 files / 551 tests**. No credentials, live provider, real business data, deployment or print-platform claim was used.
- Limitation: Playwright does not certify a separately enabled production WebMCP browser feature in this case; the browser-side WebMCP adapter registration and tool execution path used the existing controlled model-context seam. CDP is applicable only to Chromium here. Firefox's existing AGRUN CSP eval diagnostic remains an allowed runtime limitation where emitted; no functional `pageerror` or unknown console diagnostic occurred. CSP and AGRUN remain unchanged and PI-05/S18 is still open.

### Case-specific evidence: PROD-01 01-08 — Pass

- Date: 2026-09-08; source: current uncommitted worktree based on `3a1a7dbb93de5edd7984842896afdaab42a92bed`; environment: Windows Playwright Chromium, Firefox and WebKit, one worker; fixture: the real Sales Invoice legacy sample plus a synthetic second table-header component.
- The in-app Sales Invoice sample has no explicit FormSpec. The actual public `get_form_spec` response reported `mode="legacy-adapter"` before and after the human source-editor edit. The editor added a second `.prowheader` with `data-pf-table-id="secondary"`, then the rendered document remained trusted and Printable.
- Under explicit `table:default`, the ambiguous `.prowheader` column-width operation returned `SCOPE_VIOLATION` before transaction creation; revision, project hash and canonical source were unchanged. After selecting explicit `all`, the same intentional whole-document operation produced a candidate with candidate hash and valid production validation, and the real private UI Apply committed exactly one revision and updated both legacy table-header widths. Switching to `component:document-header-*` rejected a theme mutation and preserved the committed revision/hash/source.
- Evidence: `e2e/studio-v2-p0-prod01-08.spec.js` passed **3/3** across Chromium, Firefox and WebKit; the focused S06 set passed **46/46**; `npm run build:site` passed **105 files / 551 tests** and generated the site artifact; `npm run check` passed. No credentials, live provider, real business data, deployment or print-platform claim was used.
- Limitation: the second table-header is an in-memory synthetic ambiguity fixture, not a new production template catalog entry; the Harness action is deterministic and in-page, so this is real-browser transaction/scope evidence, not live-provider or PI certification. Firefox retained the existing AGRUN CSP eval console diagnostic without functional `pageerror` or unknown console diagnostics; CSP and AGRUN remain unchanged and PI-05/S18 is still open.

## Implemented corrections

| Review item | Current correction | Boundary preserved |
|---|---|---|
| R1 | Import clears sample provenance. Real/Unknown to Synthetic requires confirmation of the entire current document and uses a fresh transaction namespace | Canonical document ID, current project/revision and old durable records remain intact; no old snapshot hydration/backfill |
| R2/R3 | Missing session policy is Unknown. Index and runtime stores bind document/session/policy generation, check actual transaction admission, abort active stale work and reject late results | Old records are not silently deleted; storage failure does not redirect to another persistent sink |
| Session UI | One session-lifecycle owner guards new/open/list/controller initialization and callbacks. Stop and project changes invalidate old work | Old callbacks cannot replace current UI state; an uncertain commit is not retried or rolled back |
| R5/R6 | MCP initialization and embedded instructions prohibit restrictive screenshots and Agent raw-source replacement, and state reference/commit limits | Human editor/export authority remains separate from Agent permissions |
| R7, found during resumed browser verification | The pinned runtime ignored the constructor's systemPrompt option. DESIGNER_PROMPT now enters each consume input, the actual provider-supported location | No vendor patch or Provider change; host enforcement remains mandatory regardless of model cooperation |
| PROD-01 scope/category enforcement | Invalid or incomplete host scopes no longer normalize to document scope; the domain guard now keeps Theme/Layout/Table/Component operation categories explicit and rejects layout binding plus cross-table component reassignment | Valid document/table/component scopes, scope fingerprints and domain operation checks remain unchanged |
| PROD-01 project replacement scope | The panel resets the active scope to the whole document for import/sample/recovery replacement, while a policy-only switch preserves the selected scope | The reset is owned by the existing panel/app lifecycle; no new scope authority or transaction path was added |
| Storage status regression | A replacement document refreshes the current session list/status; an invalidated boot callback no longer suppresses storage-unavailable feedback | New chats report volatile fallback without claiming saved records were erased |
| M2/M3 output variants | The Agent projection now keeps review provenance exact, whitelists repeated-area keys, rejects malformed response envelopes and wrong-type required arrays safely, and verifies local-only Review media provenance before rebuilding final Provider parts | Canonical projects, transaction results, opaque references and human approval remain owned by the existing domain/gateway boundary; provenance markers never enter the Provider payload |
| M3 Provider final payload | `buildProviderInput()` and the exported media projection now default a missing policy to imported Unknown and apply the closed media/provenance projection even when called directly; synthetic pixel media therefore requires an explicit Synthetic policy. Runtime/layout calls can attach a policy-bound fetch that rejects stale context before the request and after an asynchronous response | User-authored prompt delivery remains explicit; no live Provider or real data was used |
| M2/M3 host admission | The public page gateway requires an exact protocol/Agent Contract/catalog admission before business execution; the CDP client performs the same preflight, obtains a memory-only document-bound token and re-admits after target replacement/reconnect; MCP `tools/list` is gated | Admission is compatibility and context binding, not authentication. WebMCP remains host-bound registration from the canonical catalog; no raw-output fallback or persisted client permission state |
| M2 transaction context | Agent-created transactions now bind document, policy generation, scope and Agent session at creation; preview, lease, recovery, read and history paths reject missing or cross-session context and filter active/history records | Reference resolution remains separate from authorization; canonical transaction IDs, revision/CAS, leases and commit outcomes are unchanged |
| M1 resource admission | `inlineProjectAssets()` defaults an omitted policy to imported Unknown and rejects fetchable template/CSS URLs before `fetch`/HEAD; current-policy checks still guard asynchronous asset work | Resource authorization stays at the asset owner; explicit file save remains a separate user action |
| M2 Agent operation admission | Strict Agent previews reject non-array/empty operation sets before transaction creation; auto-apply also requires a non-empty, fully eligible operation list | Internal non-Agent evidence may retain its empty operation anchor; public `minItems: 1` remains authoritative |
| Canonical Undo/Redo | Logical history navigation now selects the prior/next content state, writes that snapshot through the existing durable CAS and assigns a fresh monotonic `history:undo`/`history:redo` revision; reload reconstructs the logical cursor from durable revision reasons | `get_revision`, project revision, CAS and evidence invalidation remain aligned; Agent output still omits the internal project |
| Old command-bus lifecycle | Document/policy replacement deactivates the previous bus; later commands, transaction/audit/evidence writes and the actual CAS fail closed, while app listeners ignore old change/review events | Old in-flight work cannot write or relabel the new document context; recovery and human editing remain explicit |
| Recovery classification provenance | The recovery banner remains metadata-only; an explicit restore derives the current project identity and installs the payload under Unknown even when the stored label says Real or Synthetic | Old records remain intact; Synthetic persistence requires a later deliberate host classification, and no recovery label is treated as provenance |
| External Demo Gateway review | The browser default now requests an origin-bound session at `/demo/session` without a gateway key, keeps the short-lived `dmo_...` token in memory, selects provider-tool-free `envelope` mode, rejects Demo-incompatible SVG evidence, and sends only the restricted request to `/demo/v1/responses`; a first `401` refreshes once | Private `/v1/*` credentials remain server-side; the Demo session is transport-only and cannot authorize Agent commands, scope/apply, export or persistence. Host-side MCP/WebMCP tools stay local and are not serialized as Provider tools |
| PROD-03 readiness projection | Document Context now requires current `readiness.productionValid` before displaying Printable; a rendered but unreviewed revision remains Blocked until the authoritative readiness gate passes | Render lifecycle status, export controls and human production-export confirmation remain separate owners |

The policy-aware runtime session adapter uses the pinned Agrun v4 IndexedDB schema. A generic wrapper
could not guard its lazy internal database open/transaction admission. The adapter stays in the existing
session-storage owner; it does not add a service or patch global IndexedDB in production.
It preserves atomic session version checks and waits for transaction completion, not request success.
Incompatible existing schemas fail closed without deleting stores. Vendor upgrades require renewed schema/API parity checks.

## Verified runs

- Initial index lifecycle regression: 4 failed / 1 passed before correction, then 5/5 passed.
- First classification/ownership/boundary set: 24/24 unit tests passed.
- Session database tests cover request-success followed by abort, stale transaction cancellation,
  blocked/timed-out open with late success, and disposed memory stores.
- Panel lifecycle tests cover delayed creation/list/controller results, Stop, shared initialization,
  and policy change across the awaited skill step. A 36-test focused set passed.
- Serial full unit run: **95 files / 497 tests passed** after the final recipient/save corrections.
- Earlier resumed `npm run doctor`: **4/5 high-level checks passed**. Vendor integrity, the three pilot validators and the
  independent check/build portions passed, but the combined unit/build step hit the existing
  `server-transaction.test.js` crash-after-CAS case's 15-second timeout during the long run.
  An isolated rerun of that file passed **1 file / 8 tests**; this remains historical evidence.
  Static pilot validation does not certify browser layout or physical printing.
- R7/status correction: **29/29 targeted unit tests passed**, assets/site rebuilt.
- M2/M3 output-boundary correction: **3/3 focused tests passed** and the existing public matrix remained **7/7 passed**; direct transactions, history arrays, undo project omission, FormSpec extensions/business labels, neutral table labels, exact reviewedRevision handling and malformed envelopes were inspected from reconstructed outputs.
- Latest output/provenance targeted run: **35/35 tests passed across 5 files** (`visual-regression`, `production-boundary`, `agent-layout-review`, `agent-provider-media`, `agent-provider-transport`); wrong-type required arrays fail closed, restrictive media requires geometry provenance, and Provider transport receives rebuilt allowlisted parts.
- Scope boundary targeted run: **15/15 tests passed across 3 files** (`agent-scope`, `agent-entry-parity`, `gateway`); malformed/incomplete scopes fail closed, category/cross-table checks reject, and embedded/WebMCP/CDP scope decisions remain equivalent.
- Latest selector-only scope correction: `agent-scope.test.js` passes **6/6**, including rejection of a selector spanning different semantic table IDs before transaction creation; the rebuilt three-engine scope/reset browser set passes **6/6**. A single-identity legacy selector remains compatible. This is supporting PROD-01 evidence, not full case closure.
- Scope browser run: **6/6 serially passed** across Chromium, Firefox and WebKit; a selected table scope survives a policy switch and resets after an imported project replaces the document, and a FormSpec-derived component selection allows its own semantic patch while rejecting another component.
- Earlier resumed serial full unit run: **97 files / 504 tests passed** after the output-boundary correction, fixture alignment and project-replacement scope guard. `npm run build:assets`, `node scripts/build-site.mjs` and `npm run check` also passed; generated artifacts were not published. The later host-admission and transaction-context changes were covered by the prior 99/513 run; the 99/515 run is retained as historical scope evidence.
- Earlier `npm run doctor`: **5/5 high-level checks passed**, including **103 files / 537 tests**, the production build and all three static pilot validators. The old-bus lifecycle/commit-race, recovery provenance, selector-only table scope, Demo Gateway transport controls and post-commit result handling are included. This is local verification only and does not close the P0 register or authorize release; the latest S05 full serial run is recorded above as 105 files / 548 tests.
- Recovery provenance correction: **11/11 focused unit tests** and **6/6 browser controls** across Chromium, Firefox and WebKit passed; explicit recovery of records labelled Real or Synthetic installs as Unknown, preserves the old record and creates no new localStorage destination.
- Prior recovery-correction serial unit/build run before the Demo Gateway amendment: **102 files / 529 tests passed**; `build:assets` and `build:site` completed. It is retained as historical evidence, not the current test baseline.
- Latest focused policy/transaction/resource regression: **28/28 tests passed** across asset-default, exporter, explicit-save, production-boundary, transaction-context, recovery, commit-boundary and scope suites. This is supporting evidence only; it does not close the P0 register.
- Latest Provider payload regression: **31/31 tests passed** across media, transport and production-boundary suites; omitted policy defaults to Unknown, unproven media is rejected, safe geometry survives the final payload projection, and stale policy is rejected both before send and after an asynchronous response.
- Latest bounded browser smoke: **27/27 serially passed** across Chromium, Firefox and WebKit for the production boundary and final Provider wire; it rechecked policy/storage/approval/late-result controls and actual final request bodies after the projection change.
- Host-admission focused run: **20/20 tests passed across 6 files** for gateway admission, CDP compatibility/transport, MCP advertisement gating, prompt/entry parity. It covers exact catalog/version rejection, opaque admission, token invalidation, reconnect/target replacement and safe no-page behavior.
- Direct public-gateway browser migration: **47/47 Chromium tests passed**; the remaining direct Firefox/WebKit set passed **52 tests with 10 expected skips and 0 failures**. A separate production-boundary/outbound/scope-reset key set passed **20/20** across Firefox and WebKit. These are bounded client-admission and routing evidence, not full P0 closure.
- Final Provider/outbound browser wire regression: **6/6 passed** across Chromium, Firefox and WebKit; the controlled second request and closed Real-data diagnostic path were inspected after the final payload change. No live Provider was contacted.
- Transaction-context focused run: **30/30 unit tests passed across 4 files**; the updated browser boundary/recovery/scope set passed **33/33** across Chromium, Firefox and WebKit. Cross-session reads, preview rebinding, legacy records, active/history filtering and actual page lifecycle remained fail-closed.
- Canonical history focused run: **45/45 tests passed across 7 files**; durable head/get_revision/project revision stayed aligned through Undo/Redo, reload reconstructed the logical cursor, compare_revision read navigation snapshots, and a new branch commit invalidated redo. The rebuilt E14/candidate browser run passed **21/21** across Chromium, Firefox and WebKit, including visible r2/r3 navigation revisions.
- Old-bus lifecycle/commit race run: **17/17 focused tests passed**; policy expiry during the commit wait and history hash phase rejected before CAS, and a deactivated document bus rejected commands, transaction writes and direct commit attempts.
- Post-commit result handling regression: the focused commit-boundary/workflow run passed **16/16**, including a synthetic validation/reporting failure after a confirmed Apply; the runtime queried the same transaction, retained the committed revision and exposed validation as unavailable without a second commit.
- Browser lifecycle/history regression: **48/48 passed** across Chromium, Firefox and WebKit; policy/document replacement, delayed Provider results, durable Undo/Redo and E14 candidate controls remained green after the old-bus boundary.
- Demo Gateway focused unit regression: **28/28 passed** across provider transport, public gateway, Demo session lifecycle and panel lifecycle suites. The checks cover no-key session issuance, `credentials: omit`, in-memory token refresh after `401`/expiry, single-flight invalidation, Demo media rejection and final Provider request projection.
- Demo Gateway browser regression: **6/6 passed** across Chromium, Firefox and WebKit with synthetic intercepted `/demo/session` and `/demo/v1/responses` requests; the session request had no Authorization header and the final request carried only the synthetic Demo bearer, with no Provider `tools`/`tool_choice` or other disabled fields. AI settings/locale smoke passed **2/2** in Chromium after the welcome/session invalidation correction. No live endpoint was contacted.
- Current-source live Demo probe: one real Chromium Studio context reached `Printable`, observed `/demo/session` without Authorization at **201**, then observed two `/demo/v1/responses` requests at **200** with the in-memory Demo-session Authorization header; the UI turn returned idle without a failure status. No raw token, credential, response body, Apply, export or deletion was retained. This is Demo reachability evidence only and does not add direct-BYOK/provider/retention or S17 gate credit.
- Latest PROD-03 readiness correction: the context unit suite passed **5/5**, and the rebuilt E14 browser contract passed **12/12** across Chromium, Firefox and WebKit. A rendered-but-unreviewed revision remained visibly Blocked; after a passing current review, the sample's non-blocking warnings remained visible while export readiness was Ready and the human export action was enabled. The review event also restored the committed preview after candidate evidence capture. This is supporting evidence only and does not close the P0 register.
- Transaction HTTP error-boundary correction: `server-http-response.test.js` passed **4/4**, together with **8/8** SQLite transaction and **1/1** restrictive server-policy tests after the final server amendment. Captured handler logs and error responses omit raw messages/validation; fixed recovery codes survive and a transaction status string cannot become an HTTP status. Fatal-process handlers were source-reviewed, not fault-injected. These checks do not certify deployed logs, WAL, backups or OS sinks.
- Resumed verification: doctor completed **5/5**, with **104 files / 541 tests**, asset/site build and all three static pilot validators. This run preceded the final server control-code/status amendment, which has the separate **13/13** focused regression above. Rebuilt Demo browser controls passed **6/6** across Chromium, Firefox and WebKit with intercepted session/provider requests. Do not describe this as a full final-worktree doctor or complete P0 acceptance.
- Runtime event projection correction: the AGRUN/UI event contract now drops unregistered event types before UI consumers; the focused contract test covers a canary-bearing unknown type and the existing trace sanitizer remains the persistence/trace boundary. This is a local projection check, not evidence that an external runtime can emit no other content.
- Initial 33-test browser composition: **27 passed / 6 failed**. The failures reproduced missing actual
  prompt delivery and missing storage-status feedback in all three engines; they were not discarded as flakes.
- After those corrections: **12/12 browser tests passed** across Chromium, Firefox and WebKit for actual
  panel session/store/wire isolation, explicit-save/prompt disclosure, storage-denial controls and the second Provider wire request.
- Classification confirmation: **3/3 browser tests passed**. Session schema/CAS and delayed runtime open:
  **12/12 browser tests passed** in the earlier bounded run.
- Expanded 36-test composition: **36/36 passed serially** across Chromium, Firefox and WebKit with one worker.
  An earlier parallel run had 34 assertion passes and 2 Firefox browser-context teardown protocol errors
  (`_maybeDontRestoreTabs`); that runner result is retained as historical evidence, not current product failure.
- Recipient replacement controls: **6/6 passed serially** across the three engines for endpoint and credential
  replacement. The final wire request used the new recipient, excluded credentials and old prompt context,
  and the policy-aware session storage contained no prompt or credential.
- Explicit-save controls: **24/24 passed serially** across the three engines, including policy races, denied
  storage, uncertain close handling and the explicit-download path. These are bounded controls, not full
  overwrite, recovery and production-export acceptance.
- PROD-13 lifecycle cases: `e2e/studio-v2-production-boundary.spec.js` passed **24/24 serially** across the
  three engines, covering mode/document delayed Provider results, Unknown import, quota fallback, Real durable
  transaction admission and final Provider payloads. The combined lifecycle/session set passed **30/30 serially**,
  covering missing-policy callbacks, delayed IndexedDB mode/document/generation changes, asynchronous runtime
  write failure, panel session replacement and the actual wire request. These runs support 13-04 and 13-08;
  the latest full 105-test P0 sweep remains historical at **102/105**, with 13-07 failing before the Gateway origin was
  registered; that no-secret `/demo/session` 403 is superseded by the registered-origin 13-07 rerun, which passed **3/3** across all three engines and closes the P0 browser case, but not live Provider reliability or retention.

Reproduction entry points: `tests/studio-v2/agent-session-lifecycle.test.js`, `agent-session-database.test.js`,
  `agent-panel-lifecycle.test.js`, `agent-policy-owners.test.js`, `agent-prompt-policy.test.js`, `agent-output-boundary.test.js`,
and `e2e/studio-v2-classification-lifecycle.spec.js`, `studio-v2-session-store.spec.js`,
`studio-v2-panel-session-lifecycle.spec.js`, `studio-v2-provider-wire.spec.js`.
Browser tests decode actual IndexedDB/store/cache content and capture controlled final Provider bodies;
they do not rely solely on a response, a status label or a screenshot.

## S08 / PROD-03 truthful current state (G1-G5, 2026-09-09)

Status: **Done, 100%** (`G1/G2/G3/G4/G5 = 10/50/80/95/100`). Cases 03-01 through 03-08, S09/PROD-04 04-01 through 04-06, S10/PROD-08 08-01 through 08-06, S11/PROD-07 actionable Quality and S12/PROD-09 retained workspace acceptance are case-specific Passes; PI and release gates remain open.

- G1 investigation traced `CommandBus.readiness()` and `validation()`, the committed render-report owner in `render-controller.js`, the Document Context projection, Quality/export controls, layout-review receipt invalidation, UI locale refresh and the existing E14/context/render-controller/readiness tests. The eight-case freeze covered pending, rendered-but-unreviewed, failed/timeout, invalidated/late evidence, candidate, save and five-locale surface agreement states.
- Current: the initial UI installs a new `CommandBus` with `renderReport` and `reviewReceipt` unset, then schedules a browser preview; `readiness()` adds `PREVIEW_REQUIRED` and `LAYOUT_REVIEW_REQUIRED`, while `agent-document-context.js` blocks Printable when readiness errors exist. After the report arrives, the current revision still remains blocked until the review receipt is current; `request_export` remains the authoritative export-readiness projection and still requires human confirmation. The public output allowlist now preserves both readiness diagnostics.
- Target achieved: the eight case-specific browser controls capture those transitions with a synthetic fixture, preserving canonical revisions/hashes, no unauthorized download and no private/provider data. No provider, print-platform or deployment check is applicable to this local state step.
- Required checks passed: case-specific Chromium/Firefox/WebKit browser evidence plus the owning readiness/context/render-controller, UI i18n and file-export regressions. The full serial unit run also passed; physical print, live Provider and deployment evidence remain outside this step.
- Case result: `e2e/studio-v2-p0-prod03-01.spec.js` passed **3/3** serially across Windows Chromium, Firefox and WebKit. The test held the real initial preview schedule, verified static validation errors were empty while `request_export` returned `PREVIEW_REQUIRED` and `LAYOUT_REVIEW_REQUIRED`, and observed Rendering/Blocked/disabled export/Pending review. Releasing the schedule produced a current Printable render status, but Context, Quality and export stayed blocked until review; revision remained `r0` and no download occurred. The first Chromium run exposed the missing public diagnostic allowlist entry; after adding `LAYOUT_REVIEW_REQUIRED` and its regression, the focused owning set passed **32/32** and the cross-engine case passed.
- Case result: `e2e/studio-v2-p0-prod03-02.spec.js` passed **3/3** serially across Windows Chromium, Firefox and WebKit. It held initial Rendering, injected a current-revision iframe failure and a silent committed timeout, and verified Preview failed/retry, `PREVIEW_REQUIRED` plus `LAYOUT_REVIEW_REQUIRED`, blocked Context/Quality/export and disabled export; clearing each fault and using the retry action restored Printable. The first targeted run exposed candidate-report leakage, so `CommandBus.invalidateRenderReport()` now clears candidate provenance at committed preview start/failure. Focused render/controller/CommandBus/context/status tests passed **29/29** and the final `npm run build:site` passed **105 files / 556 tests**, with no functional pageerror or unknown diagnostic.
- Case result: `e2e/studio-v2-p0-prod03-03.spec.js` passed **3/3** serially across Windows Chromium, Firefox and WebKit. It verified missing review, blocking findings, unavailable AI and a passing current review; locale editing, recovery-cache draft persistence and policy-permitted untrusted export remained possible while trusted export stayed blocked until human confirmation. The first browser run exposed empty review-session initialization clearing the committed report, and the next run exposed report loss while restoring the committed iframe; `app.js` now ignores an empty candidate reset and `render-controller.js` preserves the committed report during restore. Focused render/controller/CommandBus/context/status tests passed **30/30** and the final `npm run build:site` passed **105 files / 557 tests**, with no functional pageerror or unknown diagnostic.
- Case result: `e2e/studio-v2-p0-prod03-04.spec.js` passed **3/3** serially across Windows Chromium, Firefox and WebKit. It passed review at r0, then applied a locale edit at r1 and navigated Undo/Redo to r2/r3; every new committed revision cleared the old receipt and stayed blocked until a fresh review, with trusted export disabled and no functional pageerror or unknown diagnostic.
- Case result: `e2e/studio-v2-p0-prod03-05.spec.js` passed **3/3** serially across Windows Chromium, Firefox and WebKit. It delivered r0 Studio-issued evidence to `complete_layout_review` after a locale edit committed r1; the stale completion returned `REVISION_CONFLICT`, and current r1 remained review-required/Blocked. No functional pageerror or unknown diagnostic occurred.
- Case result: `e2e/studio-v2-p0-prod03-06.spec.js` passed **3/3** serially across Windows Chromium, Firefox and WebKit. It kept a reviewed committed r0 ready while an r1 candidate was visibly distinct; the actual trusted download attestation identified r0, and Discard restored the committed preview without changing the revision or project hash. No functional pageerror or unknown diagnostic occurred.
- Case result: `e2e/studio-v2-p0-prod03-07.spec.js` passed **3/3** serially across Windows Chromium, Firefox and WebKit. It verified picker cancellation, failed write and download fallback states, required a confirmed close for Saved, and held the close while a locale edit committed r1; the completed file remained attested to r0 while current r1 stayed Unsaved and not export-ready. The revision-bound retry cache fix is covered by the focused `studio-file-export` suite **11/11**; no functional pageerror or unknown diagnostic occurred.
- Case result: `e2e/studio-v2-p0-prod03-08.spec.js` passed **3/3** serially across Windows Chromium, Firefox and WebKit. It compared Document Context, Quality, export chip/button, save/review labels and `request_export` while the real browser moved through initial Rendering, rendered-but-unreviewed blocking, warning-only reviewed readiness, a pending candidate and a human-cancelled export. Each of `en-MY`, `zh-CN`, `ms-MY`, `ja-JP` and `vi-VN` refreshed the current state without stale diagnostics; the test asserted no automatic download. Chromium first exposed stale `lastValidation` reuse during locale refresh; `app.js` now re-derives `bus.readiness()` on every UI locale event. No functional pageerror or unknown diagnostic occurred; the known Firefox AGRUN CSP diagnostic remains explicitly allowed.
- G2/G3: `app.js` now refreshes localized Quality from current `bus.readiness()` instead of a stale pre-render validation snapshot; existing render/controller/save ownership, revision/CAS, candidate separation and human confirmation boundaries remain unchanged. S10 additionally guards dirty HTML replacement and preserves newer dirty state during a held save. No protocol, persistence, provider or deployment scope was added. The S10 focused set passed **9 files / 82 tests**; the final serial unit run passed **105 files / 558 tests**, with `npm run check`, `node --check` and `node scripts/build-site.mjs` passing.
 - S16 G4/G5: S14/PI-01 and S15/PI-02 each pass **18/18** in Chromium, Firefox and WebKit; current S16/PI-03 16-01..06 pass **18/18**, and affected AGRUN/session/policy regressions pass **31/31**. S13/PROD-11 remains closed with bounded browser/full-build evidence. The isolated suites do not certify Edge, Safari.app, a physical printer, a live Provider, live CORS/preflight or deployed retention. Final S16 build passed **106 files / 567 tests**; focused PI-03 Storage passed **3/3**; PI-00..03 bundles are 823,439 / 14,270 / 1,123,184 / 867,676 bytes and SW precache is 193 entries. Current plan credit is 76.7% with 16/21 Done; no deployment, push, release approval or user-data deletion was performed.
- S16 G2/G3: the isolated public PI `StorageBackedSession`/`MemorySessionRepo` path uses policy-bound volatile memory, versioned IndexedDB, atomic CAS and an explicit same-session writer lock; legacy AGRUN records remain labeled read-only and are never hydrated. `node --check`, `npm run check`, the final entry boundary scan, the <=300-line inventory and `git diff --check` passed.
- S16 G5: the current isolated artifact is locally closed; documents now point to S17/PI-04. Production AGRUN cutover, release approval, live Provider/platform/print certification and later PI gates remain open.
## S17 / PI-04 current case evidence
- Isolated PI X-01 supporting case: **Pass 3/3** across Chromium, Firefox and WebKit; actual PI Harness, policy-bound memory, table-A scope,
  private approval/apply, r0→r1 edit, geometry review and human export passed with no canary exposure. The separate P0 X-01
  production-shell case also passed **3/3** through real UI import, Apply, review and confirmed export; its controlled in-page
  AGRUN/BYOK runtime is UI evidence only, not live Provider/PI cutover evidence. X-02/X-03 also pass **3/3** each as isolated
  policy/host/session boundaries; stale results leave revision 0 and no canary output. The combined production-shell X-01..03 command passed **9/9** across the same engines and closes the remaining P0 records. Cases 17-04 and 17-05 each add **3/3**
  direct-browser `createPiByokAdapter`/Harness composition for Chat and Responses; 17-06 adds **3/3** for Gemini, 17-07 for
  follow-up payloads, 17-08 for commit recovery and 17-09 for Real-policy privacy. Keys are header-only and bodies exclude
  key/canary. The combined PI-04 browser command passed **27/27**, and the current-source P0 X-01..03 plus PI-04 target command passed **36/36** across Chromium, Firefox and WebKit. The earlier 54/54 command included PI-03 and used the pre-a4caf93 runner/source baseline; a 2026-09-10 repeat hit Chromium/WebKit **36/36** but all Firefox cases failed before page creation with `browserContext.newPage` and teardown hung, so that repeat is environment diagnostics only. The Windows-only Firefox runner adjustment in `93a4f2055f9e30dc75f7df06c9e4ba3c1baebff0` adds `--disable-gpu` while retaining content sandboxing; the pre-a4caf93 standard 54-test command then passed **54/54** across all three engines. One preceding run was **53/54** due a Firefox teardown protocol error after page assertions passed, followed by standalone **1/1** and full **54/54** reruns. A separate Firefox-only run with `MOZ_DISABLE_CONTENT_SANDBOX=1` passed **18/18**, but it weakens browser isolation and remains diagnostic only. All nine PI cases remain supporting evidence, and candidate/committed renders now use the sandboxed iframe/geometry path.
- Current isolated PI-04 bundle: **1,212,588 bytes**, entry SHA-256 `4fdf656912895823ce1c2c67856d1e643a1a5eb4a8219b8262599a3e208f099c`; static/frontend-only manifest has `appBackend: false`,
  `providerProxy: false`, `directProviderChat: true`, `directProviderResponses: true`, `directProviderGemini: true`, `directProviderFollowUp: true`, `directProviderCommitRecovery: true`, `directProviderRealPrivacy: true`, `providerMatrixSynthetic: true`, `liveByokSmoke: false`, and SHA-256 `5ee8d01dbcb4443e6bc2a4fc0fe7b50299223a23b8c91b2787521e44363dad95`.
  `npm run check`, JS `node --check`, `git diff --check`, PI-02 **18/18**, full **106 files / 571 tests**, and the current local browser matrix **88/88** passed; rebuilt site has **203** SW precache entries. See [S17 evidence](STUDIO_V2_S17_PI04_EVIDENCE.md).
- S17 remains **G1 / 10% (`x----`)** because the current Demo Gateway run is supporting-only and the direct-BYOK provider/render/privacy/transaction matrix plus final PI-04 exit criteria remain open; current plan credit is 76.7% with 16/21 Done and gate credit remains in TASK.

## Remaining work and safe continuation
1. P0 case closure is now 35/35 after the registered-origin 13-07 browser requalification; the current Demo Gateway run is supporting transport/privacy evidence and is not direct-BYOK Provider reliability certification.
2. Continue S17 through the remaining final provider/render/privacy/transaction matrix one case at a time, reusing PI-00..04 without cutting over AGRUN.
3. Prepare S18 cutover, S19 print/platform certification, S20 packet and S21 approval only at their gates; do not deploy, publish or push.

Rollback preserves canonical committed data and existing records; never restore permissive Unknown/Real defaults,
raw output, unconditional pixels or an unguarded session store. No real business data or direct-BYOK live Provider test, deployment, release, push or user-data deletion was performed. SCMC: **PASS WITH WARNINGS** for this bounded design; pinned runtime drift requires rerunning browser parity/CAS checks. This is not Production Ready.
